import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { NodeAdmin } from "./NodeAdmin";
export declare class NodeAdminFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_link: string, _node: string): Promise<NodeAdmin>;
    getDeployTransaction(_link: string, _node: string): UnsignedTransaction;
    attach(address: string): NodeAdmin;
    connect(signer: Signer): NodeAdminFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): NodeAdmin;
}
